package com.tailor.engine.blocks;

/** PHASE3_SPEC.md section 7: the outcome of putting a library project into a position. */
public enum SwapOutcome {
    OK,
    INSUFFICIENT_BULLETS,
    MISSING_VARIANT,
    BULLET_TOO_LONG,
    HEADER_TOO_LONG,
    INVALID_LINK
}
